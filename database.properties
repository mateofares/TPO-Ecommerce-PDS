# Database configuration (update with your credentials)
db.driver=com.mysql.cj.jdbc.Driver
db.url=jdbc:mysql://localhost:3306/tpo_ecommerce?useSSL=false&serverTimezone=UTC
db.user=root
db.password=admin

# The application will create the necessary tables automatically on startup.
# If you need to create them manually, use the following schema:
#
# CREATE TABLE usuarios (
#   id BIGINT PRIMARY KEY,
#   nombre VARCHAR(100),
#   apellido VARCHAR(100),
#   email VARCHAR(150) UNIQUE,
#   contrasenia VARCHAR(255),
#   rol VARCHAR(50),
#   legajo INT NULL
# );
#
# CREATE TABLE productos (
#   id BIGINT PRIMARY KEY,
#   nombre VARCHAR(150),
#   precio DOUBLE,
#   stock INT,
#   categoria_nombre VARCHAR(100)
# );
#
# CREATE TABLE pedidos (
#   id BIGINT PRIMARY KEY,
#   fecha DATE,
#   estado VARCHAR(50),
#   metodo_pago VARCHAR(100),
#   total DOUBLE
# );
#
# CREATE TABLE pedido_items (
#   pedido_id BIGINT,
#   producto_id BIGINT,
#   cantidad INT,
#   precio_unitario DOUBLE,
#   PRIMARY KEY (pedido_id, producto_id),
#   FOREIGN KEY (pedido_id) REFERENCES pedidos(id),
#   FOREIGN KEY (producto_id) REFERENCES productos(id)
# );
